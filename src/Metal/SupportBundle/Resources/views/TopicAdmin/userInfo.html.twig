{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <div>
        {% if object.resolvedBy %}
            <p>{{ object.resolvedBy.getFullName }}</p>
        {% endif %}
    </div>
{% endblock %}